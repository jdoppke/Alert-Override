Here's a script that simply overrides JavaScript's native alert function and places a modal on the page instead, all while still keeping JavaScript's native alert available in a new function, nativeAlert().
